# Copy this file to .env.local and fill in your real key.
# Never commit .env.local.
#
# On Vercel: Project → Settings → Environment Variables → add the same keys.
OPENAI_API_KEY=

# Optional on Vercel: paste full contents of data/minh.json as one line.
# If set, overrides the bundled data/minh.json (useful if repo is public).
# CHILD_STATE_JSON=
