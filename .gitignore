# Dependencies
node_modules/

# Next.js build output
.next/
out/
*.tsbuildinfo

# Environment variables / API keys — TUYỆT ĐỐI không commit
.env
.env.local
.env*.local

# Dữ liệu cá nhân của con — repo nên để Private trên GitHub
# Seed chính nằm ở src/lib/seed-khang.ts (để Vercel build được)
data/*.json
!data/curriculum.json
!data/khang.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.vercel/
